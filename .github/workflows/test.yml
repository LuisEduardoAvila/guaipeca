# Run test suite and linter on push/PR
# Runs on bare-metal GitHub Actions runner (no container)

name: Tests

on:
  push:
    branches: [main, develop]
    paths:
      - 'projects/guaipeca/**'
      - '.github/workflows/test.yml'
  pull_request:
    branches: [main]
    paths:
      - 'projects/guaipeca/**'

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libxml2-dev libxslt-dev libffi-dev libjpeg-dev poppler-utils antiword

      - name: Install Guaipeca with dev dependencies
        working-directory: projects/guaipeca
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[all,dev]"

      - name: Lint with ruff
        working-directory: projects/guaipeca
        run: ruff check src/

      - name: Run tests
        working-directory: projects/guaipeca
        run: python -m pytest tests/ -v --timeout=120 --tb=short
        env:
          HF_HUB_OFFLINE: '0'  # Allow model download for tests